__pycache__/
.ruff_cache/
*.pyc
*.bak.json
*.prev.json
candidates_raw.json
candidates_filtered.json
candidates_enriched.json
batch/
judge/in_*.txt
judge/in_*.txt
schemas/
probes.prev/

# 생성물 — `python3 render_site.py`로 언제든 다시 나온다. 커밋하면 모든 diff가
# 49만 줄이 되어 이종 교차검증(predeploy)이 타임아웃으로 죽는다(2026-08-29 실측 rc=124).
site/
.wrangler/

# 시크릿 보관 위치를 가리키는 로컬 배선(tokens.py). **값은 안 들어간다** — 경로만이다.
# 소스에 절대경로를 박지 않기 위해 존재하므로 이 파일이 추적되면 목적이 무너진다.
.tokenpath.local
