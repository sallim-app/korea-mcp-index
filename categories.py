#!/usr/bin/env python3
"""분야 정의 — **분류가 검색어를 낳는다** (2026-08-18, D-2026W34-21/22).

설계 뒤집기(사장님 지적 2026-08-18): 처음엔 검색어를 아무렇게나 고르고 나중에 분류를
붙이려 했다. 순서가 거꾸로다. **먼저 분야를 정하고, 각 분야가 자기 검색어를 갖는다.**
그러면 세 가지가 공짜로 따라온다.
  ① 커버리지가 분야별로 측정된다 — 어느 분야를 못 봤는지 보인다.
  ② 검색어가 분류표가 된다 — 그 항목을 찾아낸 검색어가 곧 그 항목의 분야다.
     별도 분류 단계가 없으니 분류가 수집과 어긋날 수 없다.
  ③ 분야를 바꾸면 수집이 따라 바뀐다 — 표와 수집이 같은 정의를 공유한다.

키워드는 **기관명 우선**이다. mcpmoa의 `korean_apis` 실측값(data.go.kr·DART·법제처·
국토교통부 실거래가·한국부동산원 청약홈·온비드·국세청·조달청 나라장터·금융감독원…)이
한국 데이터의 실제 축을 그대로 보여준다 — 추상적 분류어보다 기관명이 정확히 찾는다.

각 질의는 GitHub 검색 1,000건 벽 아래여야 한다(실측: `mcp 한국` 91 · `mcp 법령` 33 —
한국 축은 전부 벽에 안 닿는다). 넘치면 그 분야를 쪼개라.
"""

CATEGORIES: dict[str, list[str]] = {
    "공공데이터·행정": ["공공데이터", "data.go.kr", "go.kr", "or.kr", "행정안전부", "통계청", "KOSIS", "정부24"],
    "법령·판례": ["법령", "법제처", "국가법령정보", "판례", "조례"],
    # `DART`는 뺐다 — Google Dart 언어와 충돌해 236건이 잡히며 분야를 오염시켰다(2026-08-18 실측).
    "금융·증시": ["전자공시", "opendart", "금융감독원", "KRX", "한국은행 ECOS", "코스피", "코스닥"],
    "부동산": ["부동산", "실거래", "국토교통부", "청약", "한국부동산원", "경매", "온비드"],
    "세금·재정": ["국세청", "홈택스", "세금계산서", "관세청", "조달청", "나라장터"],
    "지도·주소": ["도로명주소", "카카오맵", "네이버지도", "지오코딩", "행정동", "주소 검색", "좌표 변환", "법정동"],
    "날씨·환경": ["기상청", "미세먼지", "대기질", "날씨"],
    "교통·이동": ["대중교통", "코레일", "지하철", "버스도착", "교통정보"],
    "의료·복지": ["건강보험", "심평원", "식약처", "국민연금", "의약품"],
    "교육·문화": ["교육부", "문화재청", "도서관", "학교알리미"],
    "한국어·언어": ["표준국어대사전", "국립국어원", "형태소", "맞춤법", "한국어"],
    "커머스·생활": ["네이버", "카카오", "쿠팡", "배달", "상품정보"],
    # 아래 2개는 **실호출로 발견한 분야**다(2026-08-19, `observed.py`). 커머스·금융
    # 검색어에 걸려 들어온 서버를 채점하다가 실제 주제가 다름이 드러났다 — 분류를
    # 고치기만 하고 검색어를 안 늘리면 다음 회차에 같은 분야를 또 우연히만 줍는다.
    "미디어·뉴스": ["뉴스", "언론", "연합뉴스", "기사 검색", "속보"],
    "핀테크·인증": ["계좌인증", "본인인증", "오픈뱅킹", "간편결제", "실명조회"],
    # `디렉토리·개발자도구`는 교정 라벨로만 쓰고 검색어를 두지 않는다 — 이 축의
    # 검색어(`SaaS`·`디렉토리`)는 한국 신호가 없어 잡음만 늘린다(DART가 그랬다).
}

# 분야에 안 걸리지만 한국 MCP인 것을 잡는 광역 질의. 분류는 '기타'로 간다.
BROAD: list[str] = ["한국", "korea", "korean"]


def queries() -> list[tuple[str, str]]:
    """(검색어, 분야) 목록. 검색어는 `mcp <키워드>` 형태로 쓴다."""
    out = [(kw, cat) for cat, kws in CATEGORIES.items() for kw in kws]
    out += [(kw, "기타") for kw in BROAD]
    return out


if __name__ == "__main__":
    qs = queries()
    print(f"분야 {len(CATEGORIES)}개 · 질의 {len(qs)}개")
    for cat, kws in CATEGORIES.items():
        print(f"  {cat:<14} {len(kws)}개  {' · '.join(kws)}")
