# 순위는 어떻게 정해지나

**실제로 물어보고, 그 답을 채점한다.** 질문·호출기록·답변·채점이 전부 이 저장소에 있으니
독자가 우리 판단을 재현하고 반박할 수 있다. 아래가 그 절차 전부다.

> 2026-08-19 개정. 그 전에는 `tools/list` 지표로 블라인드 심사를 했다. 그 방식은
> 폐기했다 — 이유는 아래 「왜 지표로 줄 세우지 않나」에 실측과 함께 남긴다.

## 왜 지표로 줄 세우지 않나

처음에는 도구 수로 정렬했다. 실측이 틀렸다고 보여줬다.

| 실측 | 뜻 |
|---|---|
| 도구 125종짜리가 설명 76%·주석 0%, 82종짜리가 100%·100% | 도구 수는 **크기**지 품질이 아니다 |
| 설명·스키마·주석 평균이 100%인 서버가 6개 동점 | 비율 지표는 **포화**한다. 순위가 아니라 합격/불합격이다 |
| 설명 길이 중앙값 1위가 도구 3개짜리(652자), 125종짜리는 42자 | 중앙값은 **표본 크기를 무시**한다 |

결정타는 실제로 불러 본 뒤에 나왔다. **도구 수 1위(125종)였던 서버는 법령 도구 전체가
API 키 오류로 죽어 사실을 한 줄도 못 줬고**, 모든 지표가 100%였던 서버는 시가총액을
10배 틀리게 돌려줬다. **작동을 안 보고 매긴 순위는 순위가 아니다.**

## 절차

```
질문     분야마다 2문. 사용자가 실제로 물어볼 말로 쓰고, 공개 자료로 확인 가능해야 한다
         (questions.py — 우리 제품이 있는 분야일수록 한 서버 기능에 맞춰 쓰지 않는다)
답변     서버당 질문당 3회, 각각 독립 세션. 답변자는 Haiku
채점     분야당 1회, 채점자는 Opus. 3회차를 한꺼번에 보고 함께 채점한다
순위     채점자의 권장순위를 그대로 쓴다 — 우리가 가중치를 새로 만들지 않는다
```

호출 기록·답변은 [answers/](answers)에, 채점은 [grades/](grades)에 그대로 있다.

> **지금 게시된 순위는 아직 1회짜리다.** 3회 반복은 2026-08-19에 정한 것이고
> **2026-09-01 채점 회차부터** 적용된다. 그때까지 `answers/`에는 서버당 기록이 하나뿐이고,
> 표 아래에도 "이 회차는 서버당 1회만 물었다"가 그대로 붙어 있다. 소급해서 다시 재지
> 않는 이유는 아래 「운영자인 우리 서버는 어떻게 다루나」와 같다 — 다시 잴 거면 전원 동시에.

### 왜 답변자는 약한 모델이고 채점자는 강한 모델인가

**답변자가 강하면 서버의 결함이 지워진다.** 좋은 모델은 부실한 응답도 자기 지식으로
메워 그럴듯한 답을 만들어 내므로 서버 간 차이가 사라진다. 실제로 한 회차에서 답변 모델이
서버가 준 10배 틀린 시가총액을 **말없이 보정해** 답했다 — 그 답만 보면 서버가 정상이다.
우리가 재려는 것은 모델의 실력이 아니라 **서버가 모델에게 무엇을 주는가**다.

**채점자가 약하면 답변자의 사각을 공유한다.** 이건 실측으로 갈렸다. 채점자(Opus)가
답변의 `재범(10년 내 2회 이상)`이 2023년 개정으로 삭제된 옛 문구임을 알아보고
**서버가 아니라 모델 기원임까지 가려냈고**, 영업이익률 42.7%가 삼성전자의 역사적
밴드(5~25%) 밖이라는 근거로 서버 데이터 결함을 짚었다. 자릿수 10배 오류처럼 내부 모순으로
드러나는 것은 약한 모델도 잡지만, 저 둘은 도메인 지식이 없으면 통과한다.

### 왜 3회인가

**한 번 물어본 순위는 서버의 성질과 모델의 주사위를 구별하지 못한다.** 서버를 고정하고
같은 질문을 반복했더니 **4개 질문 자리 중 2곳에서 등급이 갈렸다**([variance/](variance)).

- 한 회차가 지역 필터를 `sigungu`만 넣어 시도 기본값이 **전국**으로 열린 채 검색하고
  그 결과를 '서울' 질문의 답으로 냈다 — 숫자는 맞고 **범위가 틀렸다**
- 3회 중 1회가 모법 조문에서 멈추고 위임된 시행령 조문에 도달하지 못해, 질문이 물은
  '어떤 경우'를 답하지 못했다

**갈림은 모델 탓만이 아니다.** 기본값이 전국으로 열리는 것도, 대표 질문의 답이 다른 이름의
도구를 한 번 더 불러야 나오는 것도 도구 설계다. **반복하지 않았으면 둘 다 못 봤다.**

## 채점 항목

질문마다 세 축을 0·1·2로 매기고, 답을 안 한 것이 정직한 거절이면 `null`로 둔다
(모르는 것을 모른다고 하는 것은 감점 대상이 아니다).

| 축 | 묻는 것 |
|---|---|
| **정확성** | 답에 적힌 사실이 실제와 맞는가. 채점자가 도구를 다시 불러 대조한다 |
| **근거성** | 답이 인용한 서버 출력이 그 답을 실제로 뒷받침하는가 |
| **완결성** | 질문이 물은 것을 덮었는가. 못 답했다면 **왜 못 하는지가 진단적인가** |

그리고 3회차를 함께 보고 하나 더 매긴다.

| 축 | 묻는 것 |
|---|---|
| **재현성** | 세 번 물어 세 번 같은 수준의 답이 나오는가. 갈렸다면 **무엇이 갈렸고 서버 설계 탓인지 모델 탓인지** |

`사실오류`는 문장을 그대로 인용해 배열로 남긴다. 순위 위쪽에 있다고 흠이 가려지면 안 되므로
표에 건수를 낸다.

## 회차 주기

| 회차 | 주기 | 하는 일 |
|---|---|---|
| 가동 | **매주** | 수집·`tools/list` 실호출·표 갱신. 이 목록이 파는 "지금 되냐"가 이것이다 |
| 채점 | **매월 1일** | 질문→답변→채점. 순위는 여기서만 바뀐다 (3회 반복은 2026-09-01 회차부터) |

**채점을 매주 하지 않는 이유**: 서버가 안 바뀌면 채점 결과도 안 바뀐다. 바뀐 게 없는
서버를 주기적으로 재호출하는 것은 새 정보가 아니라 **남의 서버에 지우는 부하**다.

채점 대상 우선순위 — ①분야별 상위 3(독자가 실제로 고르는 자리라 낡으면 목록이 낡는다)
②지난 회차와 도구 목록이 달라진 서버(고쳤다는 신호) ③새로 잡힌 서버 ④운영자 요청.

**날짜는 고정한다.** 예산이 남는 날에 돌리면 측정일이 떠다녀 회차 간 비교가 깨지고,
그러면 변화를 개선으로 읽을지 잡음으로 읽을지 판단할 근거가 사라진다. 대신 예산이 모자라면
**위 우선순위대로 줄이고, 줄인 사실을 README에 적는다** — 못 한 것을 못 했다고 적는 것이
우리가 남에게 요구하는 바로 그 규약이다.

## 운영자인 우리 서버는 어떻게 다루나

**같은 표, 같은 잣대, 같은 회차.** 그리고 하나 더 — **순위는 전원 동시에만 다시 잰다.**

우리는 지적을 받으면 그날 고치고 다시 잴 수 있고 남은 그럴 수 없다. 개별 재측정을 순위에
반영하면 우리 서버만 계단식으로 오르고 남은 자기 최악의 순간에 박제된다. 실제로 우리는
이 표의 지적을 받아 우리 서버를 고치고 다시 쟀지만 **그 결과를 순위에 넣지 않았다** —
기록은 `answers/`에 '정규 회차 아님'으로 남아 있다.

## 한계

- **우리가 질문을 골랐다.** 분야마다 2문뿐이고, 그 2문이 그 분야를 대표한다는 보장은 없다.
  질문은 공개돼 있으니(`questions.py`) 더 나은 질문을 제안해 달라
- **채점자도 모델이다.** 근거를 전부 공개하는 것으로 줄일 수는 있어도 없앨 수는 없다
- **지금 게시된 순위는 한 번 물어본 결과다.** 다시 물으면 등수가 갈릴 수 있다 —
  아래 하한값이 그 근거다. 3회 반복은 다음 채점 회차(2026-09-01)부터 적용된다
- **3회는 "갈릴 수 있다"까지만 말한다.** "몇 %로 갈린다"는 이 표본으로 못 댄다
- **잡음 측정은 우리 서버 두 대로만 했고**, 둘 다 오독 방지 표기를 많이 붙여 둔 상태다.
  따라서 그 값은 전체 잡음의 **하한**이다
- 채점이 틀렸다고 보이면 [이슈](https://github.com/sallim-app/korea-mcp-index/issues)로
  짚어 달라 — 답변·근거·채점이 전부 공개돼 있으니 어느 문장이 왜 틀렸는지 지목할 수 있다
