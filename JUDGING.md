# 순위는 어떻게 정해지나

이 목록의 분야별 순위는 **공개된 기준에 따른 블라인드 판단**이다. 아래가 그 전부다 —
심사자에게 주는 지시문, 심사자가 보는 것, 그리고 보지 못하는 것.

## 왜 지표로 줄 세우지 않나

처음에는 도구 수로 정렬했다. 그게 틀렸다는 것을 실측이 보여줬다.

| 실측 | 뜻 |
|---|---|
| 도구 125종짜리가 설명 76%·주석 0%, 82종짜리가 100%·100% | 도구 수는 **크기**지 품질이 아니다. 기능을 잘게 쪼갠 서버가 무조건 위로 간다 |
| 설명·스키마·주석 평균이 100%인 서버가 6개 동점 | 비율 지표는 **포화**한다. 순위가 아니라 합격/불합격이다 |
| 설명 길이 중앙값 1위가 도구 3개짜리(652자), 125종짜리는 42자 | 중앙값은 **표본 크기를 무시**한다 |

지표를 더 정교하게 만드는 길은 막혀 있다 — 포화하거나, 크기를 재거나, 도메인 지식이 필요하다.
남은 정직한 길은 **판단을 하되 그 판단을 검증 가능하게 공개**하는 것이다.

## 블라인드

심사자는 **이름도 저장소 주소도 보지 못한다.** `S1`·`S2` 같은 익명 번호와 설명, 측정값만 본다.
이 목록의 운영자가 만든 서버도 같은 방식으로 섞여 들어간다 —
그래야 "자기가 자기를 올렸다"는 반박이 성립하지 않는다.

**완전한 블라인드는 아니다.** 설명 본문에 브랜드 이름이 들어 있으면 심사자가 알아볼 수 있다.
그것까지 지우면 판단 근거가 사라지므로 지우지 않았다. 한계로 적어 둔다.

## 심사자가 보는 것

각 후보 한 줄: `도구 수 · 웜(ms) · 콜드(ms) · 설명 있는 비율(중앙 길이) · 스키마 비율 · 주석 비율 · 설명 본문`

## 지시문 (전문)

```
아래는 같은 분야의 MCP 서버 후보들이다. 각 줄은 탭으로 나뉜다:
익명번호 · 도구 수 · 웜 지연 · 콜드 지연 · 설명 비율(중앙 길이) · 스키마 비율 · 주석 비율 · 설명 본문

이 분야의 실제 질문에 **AI가 제대로 답하게 해 줄** 서버 3개를 순서대로 고르고,
각각 한 줄로 이유를 써라.

판단할 때 이것을 보라:
- 이 서버가 주는 것이 그 분야에서 **실제로 물어보는 것**인가. 도구가 많아도 곁가지만
  많으면 낮게 본다. 도구가 적어도 핵심을 덮으면 높게 본다.
- 모델이 그 도구를 **언제 어떻게 쓸지 알 수 있는가**. 설명·스키마·주석이 그 신호다.
  설명이 비어 있으면 데이터가 정확해도 모델이 도달하지 못한다.
- **응답이 느리면 실사용에서 버려진다.** 콜드가 웜의 몇 배인지도 본다.

이것은 보지 마라:
- 도구 개수 자체를 점수로 삼지 마라. 기능을 잘게 쪼갠 것과 넓게 덮은 것을 구분하라.
- 설명이 길다고 좋은 것이 아니다. 언제 쓰는지가 적혀 있는가로 보라.

출력은 JSON 하나. 마크다운 펜스도 설명도 붙이지 마라:
{"top":[{"id":"S1","why":"<한 줄, 한국어>"},{"id":"S2","why":"..."},{"id":"S3","why":"..."}],
 "note":"<이 분야 후보 전체에 대한 총평 한 줄, 한국어>"}
```

## 재현

심사 결과는 [`ranking.json`](ranking.json)에 캐시된다. 키는 `sha256(분야 | 이 문서 | 입력 전체)`라
**입력이나 이 기준이 바뀌지 않으면 다시 부르지 않는다** — 같은 데이터면 같은 표가 나온다.
기준을 고치면 키가 바뀌고 전 분야가 다시 심사된다.

심사 입력·결과·이유가 모두 저장소에 있으므로, 우리 판단에 동의하지 않으면
같은 입력으로 직접 판단해 반박할 수 있다.

## 후보가 적은 분야

후보가 **3건 미만**인 분야는 심사하지 않는다.

후보가 정확히 3건인 분야는 심사하되, **"고른 것"이 아니라 "줄 세운 것"**이다 —
셋 중 셋이 다 실리므로 선택은 없고 순서만 있다. 그 분야에는 그렇게 적어 둔다.
선택과 정렬은 다른 주장이고, 섞어 쓰면 목록이 실제보다 두껍게 읽힌다.

---

[← 목록으로](README.md)
