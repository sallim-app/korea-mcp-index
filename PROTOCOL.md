# 신뢰 규약 — 무엇을 재는지 먼저 말한다

이 목록은 **우리 제품이 들어 있는 순위**를 게시한다. 우리 것을 빼는 대신
재현 가능성으로 방어하기로 했고, 그 대가로 지켜야 하는 것이 이 문서다.

> **왜 빼지 않나.** 한국 부동산·법령 최상위를 빼먹은 목록은 목록이 아니다.
> 제외는 이해충돌을 줄이지도 못하면서 완결성만 깎는다. 그래서 넣되, 아래 3조를 진다.

## 3조

**① 측정 항목을 결과 보기 전에 공개하고 고정한다.**
아래 「고정된 측정 항목」이 그 전문이다. 회차 결과를 본 뒤에 항목을 바꾸지 않는다.
바꾸면 「개정 이력」에 **무엇을·왜·그래서 우리 등수가 어떻게 됐는지**까지 적는다.

**② 우리가 지는 항목을 반드시 포함한다.**
유리한 축만 재면 그 순위는 판정이 아니라 광고지다. 원격 MCP인 우리가 불리한 축
— 셀프호스팅·오픈소스·무료 한도 — 을 뺄 수 없게 여기 박아 둔다.
불리한 숫자를 남이 찾아내기 전에 우리가 먼저 싣는 것이 가장 강한 신뢰 신호다.

**③ 측정 스크립트와 원자료를 공개해 남이 다른 가중치로 재계산할 수 있게 한다.**
스크립트는 이 저장소 전부이고, 원자료는 [measured.json](measured.json)·
[axes.csv](axes.csv)·[answers/](answers)·[grades/](grades)·[variance/](variance)에 있다.
`python3 recompute.py --weights tool_count=1,warm_ms=-0.01` 로 **자기 가중치로 다시** 매길 수 있고,
`python3 recompute.py --verify` 로 **게시본 숫자가 원자료에서 재현되는지** 검사할 수 있다.

## 남는 편향 — 없앨 수 없는 것

**측정 항목을 고른 것이 우리다.** 원자료를 공개해도 이건 안 없어진다.
축을 하나 넣고 빼는 것만으로 등수는 바뀌고, 그 선택권을 쥔 쪽이 순위에 자기 제품을 올린 쪽이다.
재계산은 *주어진 축들 안에서의* 가중치를 바꿀 뿐, 애초에 재지 않은 축을 되살리지 못한다.

그래서 이 문장은 **소유 공시(🏠)와 같은 자리**에 적는다 — 각주로 미루면
"운영자도 같은 표에서 잰다"만 읽히고 "그 표를 운영자가 설계했다"는 안 읽힌다.

재지 않은 것 중 우리가 아는 것: 가격 대비 성능, 데이터 신선도, 장애 이력, 지원 응답 속도,
약관·데이터 재배포 조건. 넣어야 할 축을 알면 [이슈](https://github.com/sallim-app/korea-mcp-index/issues)로 알려 달라.

## 고정된 측정 항목

`recompute.py`의 `COLUMNS`가 이 표의 기계 정본이고, 회귀 테스트가 둘이 어긋나면 실패한다
(`tests/test_trust_protocol.py`) — **여기 없는 축은 게시할 수 없고, 게시한 축은 여기서 지울 수 없다.**

### 가동 — 매주

| 축 | 뜻 | 어떻게 재나 |
|---|---|---|
| `status` | **살아있음 확인 / 확인 못 함 / 죽음 확인** | `live` = MCP 응답을 실제로 받았다 · `unverified` = 우리 호출로 확인이 안 됐다(**죽었다는 뜻이 아니다**) · `down` = 호스트가 없다는 직접 증거(DNS 미해결·연결 거부)만 |
| `reachable` | `status`가 `live`인가 | 위 축의 파생값. 옛 재계산이 깨지지 않게 남긴다 |
| `needs_key` | 키 없이 되는가 | 무인증 호출의 401/403 여부 |
| `tool_count` | 도구가 몇 개 들어 있나 | 응답의 `tools` 길이. 0이면 껍데기 |
| `warm_ms` / `cold_ms` | 연달아 부를 때 / 첫 호출 | 콜드 1 + 웜 2, 사이에 간격 |
| `described_pct` | 설명이 붙은 도구 비율 | 응답의 `description` 유무 |
| `desc_median` | 설명 길이 중앙값 | 〃 |
| `input_schema_pct` / `output_schema_pct` | 스키마가 붙은 비율 | 응답의 스키마 유무 |
| `annotated_pct` / `readonly_pct` | 주석·읽기전용 표시 비율 | `readOnlyHint` 등 |

### 우리가 지는 항목 — 회차마다

| 축 | 뜻 | 어떻게 재나 |
|---|---|---|
| `self_hosting` | 남이 자기 것으로 띄울 수 있나 | `packaged`(배포판 확인) / `source_only`(소스만) / `unknown`(미확인) |
| `license` | 실제 오픈소스인가 | GitHub이 인정한 SPDX 식별자. 라이선스 파일이 없으면 `공란`(= 저작권 전부 유보) |
| `repo_public` | 저장소를 읽을 수 있나 | 404면 `공란` — **비공개·삭제·개명 중 하나이지 "없다"가 아니다** |
| `paid_disclosed` | 유료 게이트를 스스로 공시하나 | 서버가 무료/유료 도구 수를 응답에 밝히는가 |
| `tools_free` / `tools_paid` / `tools_total` | 무료 한도 | 위 공시가 있을 때만 |

### 순위 — 매월 1일

| 축 | 뜻 | 어떻게 재나 |
|---|---|---|
| `rank_in_category` | 분야 안 등수 | 분야별 2문항을 실제로 물어 답을 채점([JUDGING.md](JUDGING.md)) |
| `factual_errors` | 채점자가 실제와 다르다고 확인한 건수 | 〃, 문장째 [grades/](grades)에 |

**종합 1등은 내지 않는다.** 분야를 가로지르는 가중치를 우리가 정하면, 우리가 상위권인 표에서
그 설계를 반박할 방법이 없다.

## 개정 이력

**여기가 이 문서의 값이다.** 항목이 고정됐다는 주장은 바뀐 적이 없다는 뜻이 아니라
**바뀐 것이 전부 여기 적혀 있다**는 뜻이다. 추가만 하고 지우지 않는다.

| 날짜 | 무엇을 | 왜 | 우리에게 |
|---|---|---|---|
| 2026-08-18 | 최초 고정 — 가동 축 8종 + `self_hostable` | 트랙 개시 시점에 결과를 보기 전 확정 | — |
| 2026-08-19 | **순위 산정 방식을 지표 가중합 → 블라인드 채점으로 교체** | 지표가 순위를 떠받치지 못했다: 도구 수는 크기지 품질이 아니고(125종이 설명 76%·주석 0%), 비율 지표는 6개 서버가 100% 동점으로 포화했다 | **불리해짐** — 지표 순위에서 우리 서버 둘이 최상단이었고, 교체 후 우리 법령 서버는 2위로 내려갔다 |
| 2026-08-20 | 패키지 축 파서 수리 | `pip install -r requirements.txt`의 `-r`을 패키지명으로 읽어 남의 서버 15건을 `설치 불가`로 게시하고 있었다 | 남에게 유리해짐(우리 축은 변동 없음) |
| 2026-09-03 | `reachable` 두 갈래 → **`status` 세 갈래**(live/unverified/down) + 두드리개를 MCP 규격에 맞춤(initialize 핸드셰이크·307/308 추적·SSE(GET) 전송·4xx 재시도) | 우리 클라이언트가 규격을 안 지켜 거절당한 것을 **남의 서버 '응답 없음'으로 대외 공시**하고 있었다. 실측: `tools/list` 무응답 29건 중 23건이 HTTP 상태코드를 돌려줬고, 고친 두드리개로 다시 두드리니 그중 일부가 도구 목록까지 정상 응답했다 | **불리해짐** — 남의 서버가 살아나면 우리 상대 위치는 내려간다. 그래도 고친다: 남을 죽었다고 잘못 적는 것이 우리가 파는 물건(기치 ②, 못 봄 ≠ 없음)을 정면으로 부순다 |
| 2026-08-21 | `self_hostable` → `self_hosting` **정의 교정** + `license`·`repo_public` 축 신설 | 옛 축은 이름은 셀프호스팅인데 실제로는 "레지스트리에 배포 패키지가 선언돼 있나"였다. 그 정의로는 소스가 공개된 서버가 전부 '불가'로 게시된다 | **불리해짐** — 우리 서버가 `불가`에서 `source_only`로 올라가지만, 그 칸에 아래 자기공시를 같이 실어 실질은 더 나빠졌다 |
| 2026-09-07 | **한국 관련성 판정 오탐 두 건 수리** — ①`molit`에 단어 경계 (`de`**molit**`ion`이 국토교통부로 잡혔다) ②언어 목록 안의 `한국어`는 한국 신호에서 제외 | 러시아 데이터 서버(`saymon-agent/payforapi`, EGRUL/INN·러시아우편)가 설명 끝의 `🌏 EN/中文/日本語/한국어` 하나로 keep까지 올라와 **게시본에 「기타」 구역을 통째로 만들며 실렸다.** 철거업체 두 곳(시카고·영국)도 공개 원자료에 "한국 관련은 맞으나…"로 실려 있었다. 우리가 다루지도 않는 목록에 남의 서버를 싣는 것은 조용한 오탐이 아니라 **목록 범위에 대한 공개 거짓말**이다(기치 ②) | 남에게 유리해지지도 불리해지지도 않는다 — **우리 등수·축은 변동 없다.** 표에서 한국 데이터 서버가 아닌 것 1건과 분야 1개(「기타」)가 빠졌다. 축을 바꾼 것이 아니라 후보 포함 판정을 고친 것이고, 회귀 `tests/test_kr_signal_precision.py`가 두 오탐 원문을 그대로 못박는다 |
| 2026-09-14 | **측정 모집단을 두 판정기의 합으로 바꾼다** — 키워드 판정기(`filter_candidates.py`)의 `keep`만 재던 것을 `keep` + (`review`인데 LLM 분류기가 `is_data_provider`라 한 것)으로. 규칙은 `population.py` | 판정기가 둘인데 모집단은 하나만 읽고 있었다. 실측(2026-09-14): `review` 218건 중 **105건을 우리 LLM 분류기가 이미 데이터 제공형이라고 판정**해 뒀는데 한 번도 두드리지 않았고(KOSIS `042Jason/kosis-mcp`·DART `MikeVenge/krx-dart-mcp`·특허청 `Tech-curator/korean-patent-mcp`·법령 `SeoNaRu/korean-law-mcp`), 그 상태로 게시본은 잰 것의 합계 **282건을 「후보 전체」라고 적었다**. 못 본 것을 없는 것으로 적은 자리다(기치 ②) | **아직 모른다 — 다음 회차에 같이 싣는다.** 빠져 있던 105건에 우리 서버 `app.sallim/korea-stay`가 한 건 있어 이 개정은 우리에게 유리할 수 있다. 그래서 등수 변화를 이 칸에 다음 회차 결과로 채우고, 그 전까지는 안 잰 것을 [명단](https://mcp-index.sallim.app/not-measured)으로 공개한다. 나머지 104건은 남의 서버다 |

### 2026-08-19 개정에 대하여 — 이것은 ①을 어긴 것이다

숨기지 않고 적는다. **순위 방식을 결과를 본 뒤에 바꿨고, 그건 ①이 금지하는 바로 그 행위다.**
정황은 이렇다 — 옛 지표 순위는 우리 서버 둘을 최상단에 올려 두고 있었고, 그 순위가 무효라는
실측이 그 뒤에 나왔다. 바꾼 방향이 **우리에게 불리했다**는 것이 유일한 정상참작이고,
그건 변명이지 면제가 아니다. 결과를 본 뒤의 개정은 유리하든 불리하든 신뢰를 깎는다.

그래서 규칙을 하나 더 건다. **앞으로 개정은 회차 사이에만 한다** —
회차가 시작되면 그 회차 결과가 다 나올 때까지 항목을 건드리지 않고,
개정은 이 파일에 **먼저** 커밋한 뒤 다음 회차부터 적용한다.
우리에게 유리해지는 개정은 그 개정 전후로 등수가 어떻게 달라지는지를 같이 싣는다.

다음 고정 시점: **2026-09-01 채점 회차.** 그 회차에 적용되는 항목은 위 「고정된 측정 항목」이고,
이 문서는 그 회차의 결과가 나오기 전에 게시됐다.

**2026-09-14 회차에서 고친 포함 판정 둘은 적용을 다음 회차로 미뤘다 — 이번엔 규칙대로다.**
수집 회차를 돌린 *뒤에* 두 가지를 발견했다. ①게시본 「기타」 두 줄 중 하나
(`io.github.sadri-dridi/tz-asia-seoul`)가 한 저장소·한 워커로 레지스트리에 수백 건을 살포한
범용 유틸리티 묶음의 한 조각이고 한국 신호가 타임존 표기 `Asia/Seoul` 하나뿐이었다
②그 주소를 두드리다 우리 측정기가 죽은 자리에서, README 자리표시자를 우리말로 쓴 주소
(`내도메인.vercel.app`)를 영어 명단만 보는 그물이 통과시키고 있었다. **필터·그물은 이번에
고쳐 커밋했고 회귀로 박았지만(`tests/test_kr_signal_precision.py`·`tests/test_addr_gate.py`),
그 판정을 이번 회차 표에 소급하지 않았다** — 2026-09-07처럼 다시 측정하면 남의 서버 400건을
한 주에 두 번 두드리게 된다. 대신 이번 주 표에는 그 줄이 무엇인지를 같은 자리에 적었다
(`observed.SCOPE`). 다음 회차엔 모집단 밖이다.

**같은 회차에 드러난, 아직 못 고친 것 — 수집이 조용히 3건을 떨어뜨렸다.** 지난주 게시본에
있던 `TheNaham/koreafood-mcp`·`josanku/wehome-insight`·`leadbrain/korean-data-mcp`가 이번
후보 30,508건에 없는데, **셋 다 GitHub에 그대로 살아 있다(HTTP 200)**. 지난주 빠진 3건은
전부 404 실측이었던 것과 다르다 — 세상이 아니라 **우리 검색이 이번에 그것들을 안 돌려준
것**이다. "지금 되냐"를 파는 목록에서 줄이 조용히 사라지는 것은 죽었다고 잘못 쓰는 것과
같은 계열의 거짓이라(못 봄 ≠ 없음), 다음 회차 전에 **이미 게시했던 서버를 후보에서 이어받는
규칙**을 넣고 그 결과를 여기 적는다.

**2026-09-15 — 그 규칙을 넣었다(`carryover.py` · 원장 `published_history.json`).** 우리가 한 번
게시한 서버는 원장에 남고, 이후 회차의 수집 원천에 안 잡히면 후보로 되살아나 **판정·측정을
다시 받는다**. 이어받기가 면제하는 것은 판정이 아니라 **침묵**이다. 원인은 그물이 아니라
그물코였다 — GitHub 검색은 질의당 상위 100건(별 순)만 주므로, 별 0~2개짜리 서버는 같은
질의에 새 저장소가 늘어나는 것만으로 밀려난다. 수집기는 그 절단을 `truncated`로 정직하게
적어 왔지만 **어느 줄이 밀려났는지는 한 번도 말하지 않았다.**

원장은 git에 남은 게시본(`measured.json`) 회차들로 채웠다 — 우리가 기억하는 것이 아니라
**실제로 게시한 것** 430건이다. 2026-09-14 회차 자료로 규칙을 시험한 결과(적용은 다음
회차부터, 아래 참조):

| 판정 | 건수 | 내용 |
|---|---|---|
| 이어받음 | 6 | 위 3건 + `JIN-Z-pop/korea-ets-mcp`·`wowzoo/korea-weather-mcp`(다섯 다 필터에서 `keep`) + `haklaekim/public-data-lens`(저장소는 404지만 잴 주소가 있어 `review`로) |
| 소멸 확인 중 | 2 | `JungHoonGhae/gongctl`·`choihyeokbin/kakao-real-estate-mcp` — 저장소 404이고 잴 주소도 없다. **한 번 보고 끊지 않는다**(아래) |
| 개명 | 1 | `hwain-hwang/Real-Estate-Location-Analyzer_MCP` → `hwain-ai/…`. 사라진 것처럼 보였으나 개명이고 새 이름으로 이미 수집됐다 |

**404는 소멸의 증거가 아니라 징후다.** 공개 전용 자격에는 **비공개로 돌린 저장소도 404로
보인다** — 그것을 소멸로 읽으면 살아서 돌아가는 남의 서버를 우리가 영구히 지운다. 우리가
고치러 온 결함을 은퇴 규칙이 반대편에서 다시 저지르는 자리라 두 겹으로 막았다: **잴 주소가
있으면 저장소가 무엇이든 은퇴가 없고**(주소로 계속 잰다 — `public-data-lens`가 그래서
이어받기에 있다), 주소도 없을 때만 **연속 2회차 404**에서 은퇴한다. 그리고 그렇게 되살린
줄의 설명을 못 읽었으면 `drop`이 아니라 `review`다 — 없는 근거로 내리는 `drop`은 그 서버에
대한 판정이 아니라 우리가 입력을 못 구했다는 사실이고, 그 `drop`이면 이어받기가 스스로를
무효로 만든다.

마지막 줄이 이 규칙의 위험을 보여 준다. GitHub은 개명된 저장소를 301로 이어 주므로 **옛
이름도 200으로 살아 보인다** — 확인 없이 이어받으면 같은 서버가 두 줄이 된다. 그래서
이어받기 전에 지금 이름을 확인하고, 이름·저장소·**엔드포인트**로 이번 회차에 이미 있는지
본다. 저장소만 보면 안 된다: 한 저장소가 여러 서버를 내는 곳(`lead788/apick-mcp` → 9개)에서
형제 줄이 남아 있다는 이유로 이어받기가 조용히 안 걸린다. 같은 이유로 **소스 저장소의 404는
레지스트리 이름으로 게시된 줄의 사망 근거가 아니다** — 저장소는 거처이고 주소가 신원이다.

**이어받은 줄은 `drop`이어도 공개 원자료에 이름째로 싣는다.** `drop` 통을 사유별 건수로만
싣는 것은 3만 건을 간추리기 위한 타협인데, 한 번 게시한 서버가 그 통에 들어가면 독자
입장에서는 또 조용히 사라진 것이다. 회귀는 `tests/test_carryover.py`(29건)가 진다 —
산출물만이 아니라 `collect_candidates.main`·`export_candidates.main` 배선을 실제로 태운다.

**이 개정도 회차 사이에 했고, 적용은 다음 회차부터다.** 2026-09-14 회차의 측정값은 하나도
다시 계산하지 않았고 게시본(README·axes.csv·웹판)은 이 커밋으로 바뀌지 않는다. 위 여섯
줄은 **다음 가동 회차에 처음 측정된다** — 그때까지는 이 표가 시험 결과일 뿐 게시된 측정이
아니다.

**2026-09-14 개정은 회차 사이에 했고, 적용은 다음 회차부터다.** 위 규칙("개정은 회차 사이에만 · 이 파일에 먼저 커밋한 뒤 다음 회차부터 적용")을 이번에는 지켰다 — 2026-09-07 회차의 측정값은 **하나도 다시 계산하지 않았고**, 게시본에서 바뀐 것은 「후보 전체 282건」이라는 **틀린 계수기**와 안 잰 명단의 공개뿐이다. 승격된 105건은 이 커밋 시점에 한 번도 두드리지 않았고, 다음 가동 회차에 처음 측정된다.

**2026-09-07 개정은 회차 *중*에 했다 — 그것도 적는다.** 위 규칙("개정은 회차 사이에만")을 지키지 못했다. 정황은 이렇다: 주간 회차를 돌려 표를 렌더한 **뒤에** 「기타」 구역이 새로 생긴 것을 보고 원인을 찾아 필터를 고쳤고, 그 자리에서 다시 측정해 다시 렌더했다(남의 서버를 그만큼 한 번 더 두드렸다). 다음 회차까지 미루는 선택지도 있었지만, 그러면 러시아 서버가 한국 MCP 목록에 일주일간 게시된다. **순위 축이 아니라 포함 판정이고 우리 등수가 변하지 않는다**는 것이 즉시 고친 근거이며, 그래도 규칙을 어긴 것은 어긴 것이다.

## 운영자 자기공시 — 우리 서버는 이 축들에서 어디에 있나

| 축 | 우리(`app.sallim/korea-realty`, `app.sallim/contract-compass`) |
|---|---|
| `self_hosting` | `source_only` — 배포판이 없다. **그리고 소스를 클론해도 답이 안 나온다**: 도구들이 우리 비공개 데이터 API를 호출하므로 거의 전부 실패한다([우리 저장소가 스스로 밝힌 것](https://github.com/sallim-app/korea-realty#-먼저-읽을-것--이-코드만으로는-데이터가-안-나옵니다)). 축의 값보다 실질이 나쁘다 |
| `license` | `MIT` — 이 축에서는 우리가 지지 않는다. 그대로 적는다. 불리한 축만 골라 싣는 것도 축 선정 편향이다 |
| `무료 한도` | 키 없이 **하루 30콜**, 이메일 사전등록으로 100콜. 도구 47종 중 **10종이 유료**(측정된 유일한 유료 게이트가 우리 것이다) |
| `repo_public` | 공개 |

**세 번째 서버 `app.sallim/korea-stay`는 위 두 줄보다 나쁘다 — 그것도 여기 적는다(2026-09-14).**
모집단을 넓힌 첫 회차에 이 서버가 처음 표에 들어왔고, 셀프호스팅 축이 `unknown`(「배포판도
공개 소스도 확인하지 못했다」)으로 찍혔다. **그 값은 남의 서버에 쓰라고 만든 값이다** — 우리는
답을 안다: **공개 저장소가 없다**(2026-09-14 실측, `sallim-app` 공개 4건 = contract-compass ·
korea-realty · awesome-mcp-servers · korea-mcp-index). 그래서 `license`도 없다. 우리가 지는
축에서 '미확인' 뒤에 숨는 것이 ②가 막으려는 바로 그 동작이라, 게시본 운영자 칸이 이 사실을
값으로 싣는다(`observed.SOURCE_CLOSED`). 소스를 공개하면 축이 스스로 `source_only`가 되어
이 문단이 필요 없어진다.

**유료 공시가 우리뿐이라는 것은 남이 무료라는 뜻이 아니다.** 우리만 밝혔다는 뜻이고,
밝힌 대가로 표에서 유료 게이트가 보이는 것도 우리뿐이다. 그래도 이 비대칭을 그대로 둔다 —
공시한 쪽이 손해를 보는 표라면 다음 회차엔 아무도 공시하지 않는다는 반론이 가능하지만,
그 답은 "공시를 지우는 것"이 아니라 "공시하지 않은 칸을 `무료`로 읽지 않는 것"이다.

---

관련 결정: D-2026W34-21(트랙 개시) · D-2026W34-22 ②③(이 규약의 원문) · D-2026W34-25(순위 방식) ·
[JUDGING.md](JUDGING.md)(순위 절차 전문) · [README.md](README.md)(측정 결과)
